# MCP Gateway K8s Configuration
# Copy to config.env and fill in your values:
#   cp config.env.example config.env
#
# This file is .gitignored -- your secrets stay local.

# --- Telegram Bot ---
# Create via @BotFather on Telegram
TELEGRAM_BOT_TOKEN=

# --- OpenClaw Gateway ---
# Random token for gateway API auth (generate with: openssl rand -hex 32)
OPENCLAW_GATEWAY_TOKEN=

# --- Ollama Model ---
# Model to pull and serve (must fit your GPU VRAM)
OLLAMA_MODEL=llama3.3:70b

# --- Tailscale Operator ---
# OAuth client for the Tailscale K8s Operator
# Create at https://login.tailscale.com/admin/settings/oauth
# Scopes: devices (write), auth_keys (write) — Tag: tag:k8s
TAILSCALE_OAUTH_CLIENT_ID=
TAILSCALE_OAUTH_CLIENT_SECRET=

# --- Notion MCP Server ---
# External URL via Tailscale Funnel (set by operator: https://notion-mcp.<tailnet>.ts.net)
NOTION_BASE_URL=https://notion-mcp.your-tailnet.ts.net
# From https://www.notion.so/my-integrations
NOTION_OAUTH_CLIENT_ID=
NOTION_OAUTH_CLIENT_SECRET=
NOTION_SESSION_SECRET=

# --- Google Calendar MCP Server ---
# External URL via Tailscale Funnel (set by operator: https://gcal-mcp.<tailnet>.ts.net)
GCAL_BASE_URL=https://gcal-mcp.your-tailnet.ts.net
# From https://console.cloud.google.com/apis/credentials
GCAL_OAUTH_CLIENT_ID=
GCAL_OAUTH_CLIENT_SECRET=
GCAL_SESSION_SECRET=

# --- Gmail MCP Server ---
# External URL via Tailscale Funnel (set by operator: https://gmail-mcp.<tailnet>.ts.net)
GMAIL_BASE_URL=https://gmail-mcp.your-tailnet.ts.net
# From https://console.cloud.google.com/apis/credentials
GMAIL_OAUTH_CLIENT_ID=
GMAIL_OAUTH_CLIENT_SECRET=
GMAIL_SESSION_SECRET=

# --- LinkedIn Scheduler MCP Server ---
# External URL via Tailscale Funnel (set by operator: https://linkedin-mcp.<tailnet>.ts.net)
LINKEDIN_BASE_URL=https://linkedin-mcp.your-tailnet.ts.net
# From https://www.linkedin.com/developers/apps
LINKEDIN_OAUTH_CLIENT_ID=
LINKEDIN_OAUTH_CLIENT_SECRET=
LINKEDIN_SESSION_SECRET=

# --- Pal-E Billing (Stripe) ---
# Restricted API key from https://dashboard.stripe.com/apikeys
STRIPE_API_KEY=
# Webhook signing secret from https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=
