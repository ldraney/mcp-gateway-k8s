apiVersion: v1
kind: Service
metadata:
  name: notion-mcp-remote
  namespace: openclaw
spec:
  type: LoadBalancer
  selector:
    app: notion-mcp-remote
  ports:
    - port: 8000
      targetPort: 8000
